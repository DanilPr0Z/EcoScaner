/**
 * Что показывать из внутренней кухни распознавания.
 *
 * Блок «Что увидела нейросеть» и страница «Модель» нужны нам, пока идёт
 * доработка модели, но пользователю не адресованы. По умолчанию они скрыты;
 * включаются на время отладки:
 *
 *     echo "VITE_SHOW_DEV_INFO=true" >> frontend/.env.local
 *
 * Перед продом флаг просто остаётся выключенным — код удалять не нужно.
 */
export const SHOW_DEV_INFO = import.meta.env.VITE_SHOW_DEV_INFO === "true";
