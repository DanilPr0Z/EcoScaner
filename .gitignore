.venv/
.idea/
__pycache__/
*.py[cod]
.pytest_cache/
.env
*.db
# Служебные файлы SQLite в режиме WAL — под *.db не подпадают.
*.db-wal
*.db-shm
*.db-journal

# Локальная конфигурация Claude Code — не часть проекта.
.claude/

# Исходники дизайна (экспорт Claude Design) — контент перенесён в код.
# Шаблон покрывает и повторные выгрузки: «(1)», «(2)» и т.д.
ЭкоСканер_ план разработки*/

# Веса YOLO — ultralytics скачивает их сам при первом запуске (~84 МБ).
*.pt

# Датасет RealWaste (666 МБ) и артефакты обучения.
realwaste-main/
datasets/
runs/
extra-classes/*/
