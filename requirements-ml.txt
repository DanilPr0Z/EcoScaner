# Зависимости распознавания. Нужны только при CLASSIFIER=ml —
# ставятся отдельно, чтобы базовая установка не тянула torch.
#
#   pip install -r requirements-ml.txt
#
# Веса модели ultralytics скачает сам при первом запуске (~84 МБ).
ultralytics>=8.4.0
pillow>=11.0.0
